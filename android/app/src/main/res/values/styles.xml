<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>
    
    <!-- Base theme for the app after splash screen -->
    <style name="Theme.AnimatedSplash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Your main app theme properties -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <!-- Splash screen theme -->
    <!-- Removed duplicate Theme.AnimatedSplash.MySplash style to fix build error -->
</resources>